# Runtime environment files stay outside /nix/store.
# Outbound Git authentication uses the relay process's ambient Git/SSH environment.
SSH_AUTH_SOCK=/run/user/1000/ssh-agent.sock
GIT_SSH_COMMAND=ssh -F /etc/git-relay/ssh_config
